version: 2
models:
  - name: dim_customers
    description: "Dimensión de clientes"
    columns:
      - name: customer_id
        tests: [unique, not_null]
sources:
  - name: raw
    tables:
      - name: orders
      - name: customers
